// Remove Key from object in an array

// export function deleteKey(array) {
//   let output = []
//   for (let i = 0; i < array.length; i++) {
//     if (array[i].sideA.length > 0) {
//       entities[i].sideA[0].songs.map(song => {
//         delete song.id;
//       });
//       modified_entities.push(entities[i]);
//     }
//   }
//   return output
// }
